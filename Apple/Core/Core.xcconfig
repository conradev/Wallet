#include "../Configuration/Framework.xcconfig"

PRODUCT_NAME = WalletCore

SWIFT_INCLUDE_PATHS = $(inherited) $(PROJECT_DIR)/Core/Constants
GCC_PREPROCESSOR_DEFINITIONS = APP_BUNDLE_IDENTIFIER=$(APP_BUNDLE_IDENTIFIER) APP_GROUP_IDENTIFIER=$(APP_GROUP_IDENTIFIER) VIEW_SERVICE_BUNDLE_IDENTIFIER=$(VIEW_SERVICE_BUNDLE_IDENTIFIER)
