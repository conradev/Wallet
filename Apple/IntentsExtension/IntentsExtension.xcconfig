#include "../Configuration/Extension.xcconfig"

PRODUCT_NAME = WalletIntentsExtension
PRODUCT_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).intents-extension
INFOPLIST_FILE = IntentsExtension/IntentsExtension-Info.plist
